package com.zyrtec.kiteflying.view.ui{	import flash.display.*; 		public class SmokeParticle	{			public var clip : DisplayObject;				public var xVel : Number = 0; 		public var yVel : Number = 0;				public var drag : Number = 1; 				public var gravity : Number = .7; 		public var side : String = "left"; 				public var shrink : Number = 2; 		public var fade : Number = 0; 				// This is the constructor, a special function used 		// to create this particle object.				public function SmokeParticle(symbolclass : Class, target : DisplayObjectContainer, xpos : Number, ypos : Number)		{			// make the particle clip			clip = new symbolclass();						// add it to the target (usually the stage)			target.addChildAt(clip, target.numChildren-1); 						// and move it to its starting position			clip.x = xpos; 			clip.y = ypos; 								}				public function update() : void		{			// add the velocity to the clip's position			clip.x += xVel; 			xVel	+= side == "right"	? -.2 : .2;			clip.y += yVel; 						// apply drag			xVel *= drag; 			yVel *= drag; 						yVel -= gravity; 						clip.scaleX *= shrink;			clip.scaleY *= shrink; 						clip.alpha -=fade; 					}				// take the clip off the stage		public function destroy():void		{			clip.parent.removeChild(clip); 					}//end destroy			}//end class}//end package